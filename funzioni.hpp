# pragma once

double mappa_in_nuovo_intervallo(double a,double b,double x);
/* dati a e b parametri di una trasformazione lineare e x dato di input 
restituisce come output il valore di x mappato nel nuovo intervallo (y)*/

double calcola_media(unsigned int n, double m, double y);
/* dato il passo (n) dell'iterazione, il valore della media (m) al passo precedente (n-1) e il nuovo valore di input (y) 
restituisce come output la media dei primi n valori del testo letto*/